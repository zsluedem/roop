# Redis Queue Consumer Configuration

# Upstash Redis connection credentials
# Get these from your Upstash Redis dashboard
UPSTASH_REDIS_REST_URL=https://your-redis-instance.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_redis_token_here

# Cloudflare R2 Configuration
# Get these from Cloudflare Dashboard > R2 > Manage R2 API tokens
R2_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com
R2_ACCESS_KEY=your_r2_access_key_here
R2_SECRET_KEY=your_r2_secret_key_here
R2_BUCKET=your-bucket-name
R2_PUBLIC_URL=https://your-custom-domain.com
# Worker Configuration
# How often to poll for new tasks (in seconds)
POLL_INTERVAL=1.0

# Directory Configuration
# Where to temporarily download images (will be created if doesn't exist)
DOWNLOAD_DIR=./downloads
# Where to save face swap outputs (will be created if doesn't exist)  
OUTPUT_DIR=./output

# API Configuration for task status updates
# Base URL of your Next.js application
API_BASE_URL=https://aifacesswap.com
# API key for authenticating worker requests (generate a secure random string)
WORKER_API_KEY=your_secure_worker_api_key_here