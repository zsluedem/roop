# Minimal CPU requirements for face swapping functionality
# Reduced from 154 packages to just the essential ones

# Core ML libraries for face detection and swapping
insightface==0.7.3
onnxruntime>=1.17.0  # CPU execution provider for ONNX models
opencv-python==4.8.0.74  # Image/video processing
numpy>=1.24.3  # Array operations

# TensorFlow for GPU memory management (even on CPU)
tensorflow>=2.16.1

# Content safety
opennsfw2==0.10.2  # NSFW content detection

# Essential support libraries
tqdm>=4.65.0  # Progress bars
requests>=2.32.3  # HTTP requests for model downloads
pillow>=10.0.0  # Image processing
protobuf>=4.23.4  # Protocol buffers for model data

# Note: This file contains only the essential dependencies for:
# python run.py -s source.jpg -t target.jpg -o output.jpg --frame-processor face_swapper
# 
# Removed 144 unnecessary packages including:
# - Web server dependencies (FastAPI, Celery, Redis)
# - GUI dependencies (CustomTkinter, tkinterdnd2)
# - Development tools (YAPF, mypy)
# - Unused ML libraries (GFPGAN, BasicSR, torchvision)
# - CUDA/GPU packages (nvidia-* libraries)
# - And many other unused dependencies